threshold = 50

for each student in class:
    avg = calculateAverage(student.marks)

    if avg < threshold:
        print("Weak student:", student.name)
    else:
        print("Satisfactory performance:", student.name)
